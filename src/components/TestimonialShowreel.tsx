'use client';

import Section from './Section';

export default function TestimonialShowreel() {
    return (
        <Section id="testimonials" className="bg-gray-900 text-white hidden">
            <div className="max-w-7xl mx-auto text-center">
                <h2 className="text-3xl font-serif mb-8">What Partners Say</h2>
                <div className="h-64 bg-gray-800 flex items-center justify-center rounded">
                    <p className="text-gray-500 italic">Testimonial Showreel Placeholder</p>
                </div>
            </div>
        </Section>
    );
}
